services:
  adminer:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./adminer-readonly.php:/var/www/html/index.php:ro
      - ../Backend/Database_backend/focuslens.db:/var/www/html/focuslens.db:ro